import { NgclassdirDirective } from './ngclassdir.directive';

describe('NgclassdirDirective', () => {
  it('should create an instance', () => {
    const directive = new NgclassdirDirective();
    expect(directive).toBeTruthy();
  });
});
